一张 16:9 技术分享幻灯片,赛博终端风格(cyberpunk terminal UI):极深的蓝黑色背景(#0a0e1a 附近),细微的网格纹理和极淡的水平扫描线质感,左上有仿终端窗口的三个小圆点装饰。主强调色为霓虹青色(#5ef1ff),次强调色为霓虹品红(#ff78d6),代码和标签使用等宽字体风格,中文正文使用现代无衬线字体,整体像一个高级开发者工具的界面。

幻灯片内容(所有文字必须准确清晰渲染,简体中文):

大标题(顶部,大号,「渐进式读取」四字用霓虹青色发光效果,其余白色):
渐进式读取 · 先查目录,再翻书

副标题(标题下方,浅灰色小字):
用户说一句「改评价模板文案库的批量重评分」,Flow2Spec 走四步——先查目录,再翻书

主体:横向排列的 4 个流程卡片,卡片之间用青色箭头 → 连接。每张卡片深色半透明底、细霓虹描边、圆角:

卡片1(青色描边)- 顶部小标签「match · 匹配」,中间等宽字体两行「m-product-review-template-library」,底部小字「命中 matcher,只读这一个分片」

卡片2(品红描边)- 标签「expand · 展开」,中间大字「+3 依赖 topic」,底部小字「common / platform / toClient 自动拉齐」

卡片3(紫色描边)- 标签「verify · 校验」,中间大字「缺口检查」,底部小字「置信度不足 → 先向用户澄清」

卡片4(琥珀色描边)- 标签「act · 执行」,中间大字「开始改代码」,底部小字「预计 3 处文件 · 带硬约束」

底部:一条居中的金句引用,浅灰色文字中嵌青色高亮:
加载什么,就是不加载什么 —— 4.7 MB 源码 → 300 行 topic,不是让 AI 更聪明,是把噪声切掉。

右下角小字页码「06 / 16」,左下角等宽小字「feature · progressive-load」。

构图要求:信息层级清晰,大量呼吸留白,四张卡片等宽横排居中,文字锐利可读无模糊,不要出现英文乱码或错别字,不要照片素材,纯 UI 平面设计。