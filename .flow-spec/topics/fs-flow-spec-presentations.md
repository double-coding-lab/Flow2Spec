# flow-spec-presentations

## 执行边界

- 本主题用于说明 **本仓库内** flow-spec **对外介绍用 HTML 演示稿** 的位置、打开方式与维护约定。
- **禁止**将演示稿正文当作 `req-docs` 去驱动业务代码实现；演示材料归属 `stock-docs` 索源说明（见下「关联文档」）。

## 目标

当用户询问「对外路演幻灯在哪」「介绍 flow-spec 的 deck」「presentations 目录做什么」时，命中本主题后：

1. 指向仓库内 `presentations/flow-spec-intro-public/`（中文）与 `presentations/flow-spec-intro-public-en/`（英文），主入口均为 `index.html`。
2. 说明其依赖 `.claude/skills/html-ppt/assets/` 的相对引用关系，避免误迁路径导致断链。
3. 需要架构级背景时，可再读 `fs-flow-spec-architecture` 主题。

## 下一步

- 索源与路径表：[对外介绍演示](../stock-docs/flow-spec-对外介绍演示.md)
- 架构总览：`.flow-spec/topics/fs-flow-spec-architecture.md`
