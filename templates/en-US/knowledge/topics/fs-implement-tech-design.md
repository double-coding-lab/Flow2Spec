# implement-tech-design (routing summary)

> **Only long-form source**: Cursor / Claude use configuration-root **`rules/fs-implement-tech-design.md(c)`** as authoritative.
> **Codex**: do not read `rules/`; execute the equivalent constraints in **`.codex/fs-rules/fs-implement-tech-design.md`** (automatically mirrored from template `rules` by `flow-spec init`).

## Purpose

- Anchors topic id **`implement-tech-design`** for `manifest-routing.topicPaths` and `index.md`.
- Keeps only **path and role** reminders to avoid maintaining a duplicate long-form body alongside `rules/`.

## Paths and Roles (must match the rules)

- Technical-spec input: `.flow-spec/req-docs/*.md` (and Markdown generated from PDFs by `fs-doc-pdf` into the same directory).
- Existing knowledge deposits: `.flow-spec/stock-docs/` — **not** direct input for "implement code from a spec".

## What to Read Next

| Environment | Next step |
| --- | --- |
| Cursor / Claude | Open or @ **`rules/fs-implement-tech-design`**, then follow its steps. |
| Codex | Read **`.codex/fs-rules/fs-implement-tech-design.md`**. |
